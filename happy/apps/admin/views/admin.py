#!/usr/bin/env python#-*- coding: utf-8 -*-from apps.common.http_util import render,redirectfrom django.conf import settingsdef login(request):    """    登陆画面    """    data = {}        return render("admin/login.html", data, request)def left(request):    """    左侧菜单    """    data = {}        return render("admin/left.html", data, request)def middle(request):    data= {}        return render("admin/middle.html", data, request)def main(request):    """    后台主页    """    data= {}        return render("admin/main.html", data, request)def sign(request):    """    先把登陆写的简单一点    """    user_name = request.POST.get('theAccountName')    pass_word = request.POST.get('theAccountPW')    if user_name == "admin" and pass_word == settings.ADMIN_PASSWORD:        data = {}        return redirect("/admin/main/")